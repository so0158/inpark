server:
    port: 9001
    servlet:
        context-path: /
        encoding:
            charset: UTF-8
            enabled: true
            force: true


# spring:
    # datasource:
    #     url: jdbc:postgresql://localhost:5432/inpark
    #     username: ssyoung
    #     password: astems1027
    #     driver-class-name: org.postgresql.Driver

    # jpa:
    #     hibernate:
    #         ddl-auto: update        # 엔티티 변경 사항을 데이터베이스에 자동 반영 (none, validate, update, create, create-drop 중 선택)
    #     show-sql: true            # 실행되는 SQL 문장을 콘솔에 출력 (디버깅용)